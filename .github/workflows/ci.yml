name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pytest pytest-asyncio httpx

      - name: Run tests
        env:
          # Minimal env so Settings() validation passes in tests.
          # The directories are created below so health/admission tests see
          # dependency readiness rather than phantom missing paths.
          R2_ENDPOINT: "https://test.r2.cloudflarestorage.com"
          R2_ACCESS_KEY_ID: "test-key"
          R2_SECRET_ACCESS_KEY: "test-secret"
          OPENROUTER_API_KEY: "test-or-key"
          OPENROUTER_PRIMARY_MODEL: "anthropic/claude-3-5-sonnet"
          OPENROUTER_SECONDARY_MODEL: "anthropic/claude-3-haiku"
          OPENROUTER_TRIAGE_MODEL: "openai/gpt-4o-mini"
          RMS_SEO_REPO_PATH: "/tmp/fake-seo"
          RMS_WEBSITE_REPO_PATH: "/tmp/fake-website"
          RMS_DRY_RUN: "true"
          RMS_LIVE_WRITE_ENABLED: "false"
        run: |
          mkdir -p /tmp/fake-seo /tmp/fake-website
          python -m compileall -q repo_mgmt tests
          python -m pytest tests/ -v --tb=short

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install linters
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install ruff mypy
      - name: Ruff check
        run: ruff check .
      - name: Mypy
        run: mypy repo_mgmt/

  docker:
    name: Docker build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build --target runtime -t rms:ci .
